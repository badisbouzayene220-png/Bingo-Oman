(function(){
'use strict';if(!/bingo-delivery-(control|admin)\.html$/i.test(location.pathname))return;const sb=window.sb,$=s=>document.querySelector(s);
function style(){if($('#bingo-leader-style'))return;const x=document.createElement('style');x.id='bingo-leader-style';x.textContent=`.bingo-leader{margin-top:18px}.bingo-leader-head{display:flex;justify-content:space-between;align-items:center;gap:12px}.bingo-leader-list{display:grid;gap:9px;margin-top:12px}.bingo-leader-row{display:grid;grid-template-columns:50px 1fr 110px 100px 120px;gap:10px;align-items:center;padding:12px;border:1px solid #e6ebf1;border-radius:14px;background:#fff}.bingo-rank{font-size:22px;font-weight:950}.bingo-level-tag{display:inline-block;padding:5px 9px;border-radius:999px;background:#0d2748;color:#fff;font-size:11px;font-weight:900}.bingo-leader-muted{font-size:11px;color:#748196}@media(max-width:760px){.bingo-leader-row{grid-template-columns:42px 1fr 85px}.bingo-leader-row .hide-mobile{display:none}}`;document.head.appendChild(x)}
function ensure(){if($('#bingo-admin-leaderboard'))return;const app=$('#admin-app');if(!app)return;const s=document.createElement('section');s.id='bingo-admin-leaderboard';s.className='bd-card bingo-leader';s.innerHTML='<div class="bingo-leader-head"><div><h2 style="margin:0">🏆 BINGO Driver Leaderboard</h2><div class="admin-muted">المستويات، التقييم، التوصيلات وBonus قيد المراجعة</div></div><button class="bd-btn ghost" id="bingo-leader-refresh">تحديث</button></div><div id="bingo-leader-list" class="bingo-leader-list"><div class="admin-muted">جاري التحميل...</div></div>';app.appendChild(s);$('#bingo-leader-refresh').onclick=load}
function medal(n){return n===1?'🥇':n===2?'🥈':n===3?'🥉':'#'+n}function money(v){return Number(v||0).toFixed(3)+' OMR'}
function render(rows){ensure();const host=$('#bingo-leader-list');host.innerHTML=rows.length?rows.map(x=>`<div class="bingo-leader-row"><div class="bingo-rank">${medal(Number(x.rank_no))}</div><div><strong>${x.display_name||'BINGO Driver'}</strong><div class="bingo-leader-muted">⭐ ${Number(x.rating||0).toFixed(2)} • ${Number(x.five_star_count||0)} تقييمات 5 نجوم</div></div><div><span class="bingo-level-tag">${x.level||'Bronze'}</span></div><div class="hide-mobile"><strong>${Number(x.total_deliveries||0)}</strong><div class="bingo-leader-muted">توصيل</div></div><div class="hide-mobile"><strong>${money(x.pending_bonus)}</strong><div class="bingo-leader-muted">Bonus pending</div></div></div>`).join(''):'<div class="admin-muted">لا يوجد مندوبون بعد.</div>'}
async function load(){try{const s=await sb.auth.getSession();if(!s?.data?.session)return;const r=await sb.rpc('admin_delivery_rewards_leaderboard');if(r.error)throw r.error;let rows=r.data;if(typeof rows==='string')rows=JSON.parse(rows||'[]');render(Array.isArray(rows)?rows:[])}catch(e){if(!/does not exist/i.test(String(e.message||'')))console.warn('BINGO leaderboard unavailable',e)}}
function boot(){style();ensure();load();setInterval(()=>{if(!$('#admin-app')?.classList.contains('admin-hidden'))load()},20000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,900),{once:true});else setTimeout(boot,900);
})();