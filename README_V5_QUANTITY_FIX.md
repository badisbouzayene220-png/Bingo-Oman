# V5 Quantity Fix

This version replaces inline quantity handlers with explicit event listeners, adds a cache-busting script version, and forces the quantity field to be a normal editable text input.

No SQL changes are required.

After uploading, open the ERP with Ctrl+F5. If using GitHub Pages, wait for deployment to finish.
