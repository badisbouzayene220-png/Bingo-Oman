(function(){
  'use strict';
  if(window.__bingoCustomerTrackingV4Loaded)return;
  window.__bingoCustomerTrackingV4Loaded=true;

  const sb=window.sb;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const statusLabels={pending:'تم استلام الطلب',confirmed:'تم تأكيد الطلب',preparing:'قيد التجهيز',ready:'جاهز للاستلام',assigned:'تم تعيين المندوب',picked_up:'استلم المندوب الطلب',on_delivery:'المندوب في الطريق',delivered:'تم التسليم',cancelled:'تم إلغاء الطلب'};
  const steps=['pending','preparing','ready','assigned','picked_up','on_delivery','delivered'];
  const stepLabels=['تم الطلب','قيد التجهيز','جاهز','تم تعيين المندوب','تم الاستلام','في الطريق','تم التسليم'];
  let realtimeChannel=null, reloadTimer=null, loading=false;

  function addStyles(){
    if(document.getElementById('bingo-customer-tracking-style-v4'))return;
    const s=document.createElement('style');s.id='bingo-customer-tracking-style-v4';
    s.textContent=`.bct-card{margin-top:18px}.bct-head{display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap}.bct-head h2{margin:0}.bct-refresh{border:0;border-radius:11px;padding:9px 13px;font-weight:800;cursor:pointer;background:#eef4fb;color:#153d70}.bct-refresh.busy{opacity:.65;pointer-events:none}.bct-live{font-size:12px;color:#187845;font-weight:800;margin-inline-start:8px}.bct-order{border:1px solid #e3e9f1;border-radius:18px;padding:16px;margin-top:14px;background:#fff}.bct-top{display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap}.bct-no{font-size:18px;font-weight:900}.bct-status{padding:6px 10px;border-radius:999px;font-size:12px;font-weight:800;background:#fff3e4;color:#a85a00}.bct-status.done{background:#e9f8ef;color:#187845}.bct-status.cancel{background:#ffeaea;color:#a83232}.bct-progress{display:grid;grid-template-columns:repeat(7,1fr);gap:6px;margin:16px 0}.bct-step{position:relative;text-align:center;font-size:11px;color:#8893a3;padding-top:27px}.bct-step:before{content:'';position:absolute;top:6px;left:50%;transform:translateX(-50%);width:16px;height:16px;border-radius:50%;background:#d9e1eb;border:3px solid #fff;box-shadow:0 0 0 1px #cbd5e1}.bct-step.done{color:#183b65;font-weight:800}.bct-step.done:before{background:#1c7be5}.bct-step.current:before{background:#ff8a1f;box-shadow:0 0 0 4px rgba(255,138,31,.15)}.bct-step:not(:last-child):after{content:'';position:absolute;top:13px;width:calc(100% - 20px);height:2px;background:#d9e1eb;right:calc(-50% + 10px)}.bct-step.done:not(:last-child):after{background:#1c7be5}.bct-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.bct-info{background:#f7f9fc;border-radius:13px;padding:11px}.bct-info small{display:block;color:#7a8798;margin-bottom:5px}.bct-driver{margin-top:12px;padding:13px;border-radius:14px;background:#eef6ff;border:1px solid #d7e8fb}.bct-driver-top{display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap}.bct-driver strong{font-size:16px}.bct-driver-meta{display:flex;gap:10px;flex-wrap:wrap;color:#66758a;font-size:13px;margin-top:7px}.bct-map-link{display:inline-block;margin-top:9px;text-decoration:none;background:#173f70;color:#fff;border-radius:10px;padding:9px 12px;font-weight:800;font-size:13px}.bct-empty{text-align:center;padding:24px;color:#778496;background:#f8fafc;border-radius:14px;margin-top:12px}.bct-error{padding:14px;border-radius:12px;background:#fff1f1;color:#a83232;margin-top:12px}.bct-note{font-size:12px;color:#778496;margin-top:10px}@media(max-width:760px){.bct-progress{grid-template-columns:1fr;gap:0;margin-inline-start:8px}.bct-step{text-align:start;padding:8px 34px 8px 0}.bct-step:before{top:8px;right:0;left:auto;transform:none}.bct-step:not(:last-child):after{top:24px;right:7px;width:2px;height:22px}.bct-grid{grid-template-columns:1fr}}`;
    document.head.appendChild(s);
  }
  function ensureSection(){let section=document.getElementById('bingo-customer-tracking');if(section)return section;const orders=document.getElementById('delivery-orders');if(!orders)return null;section=document.createElement('section');section.id='bingo-customer-tracking';section.className='bd-card bct-card';section.innerHTML='<div class="bct-head"><div><h2>متابعة الطلب <span id="bct-live" class="bct-live">● مباشر</span></h2><div class="bd-muted">تابع حالة طلبك والمندوب خطوة بخطوة</div></div><button id="bct-refresh" class="bct-refresh" type="button">تحديث</button></div><div id="bct-content"><div class="bct-empty">جاري تحميل طلباتك...</div></div><div id="bct-last-check" class="bct-note"></div>';orders.closest('section')?.after(section);const fakeMap=document.querySelector('.bd-map');if(fakeMap?.closest('section'))fakeMap.closest('section').style.display='none';section.querySelector('#bct-refresh').onclick=()=>loadTracking(false);return section;}
  function currentIndex(status){if(status==='confirmed')return 0;const i=steps.indexOf(status);return i<0?0:i;}
  function timeline(status){if(status==='cancelled')return '<div class="bct-status cancel" style="margin-top:14px;display:inline-block">تم إلغاء الطلب</div>';const idx=currentIndex(status);return '<div class="bct-progress">'+stepLabels.map((label,i)=>`<div class="bct-step ${i<idx?'done':i===idx?'done current':''}">${label}</div>`).join('')+'</div>';}
  function driverBlock(row){if(!row.driver_id)return '<div class="bct-driver"><strong>المندوب</strong><div class="bct-driver-meta"><span>لم يتم تعيين مندوب لهذا الطلب بعد</span></div></div>';const online=row.driver_online===true?'🟢 متصل':'⚫ غير متصل';const phone=row.driver_phone?`<span>📞 ${esc(row.driver_phone)}</span>`:'';const vehicle=row.vehicle_type?`<span>🛵 ${esc(row.vehicle_type)}</span>`:'';const rating=row.driver_rating!=null?`<span>⭐ ${Number(row.driver_rating).toFixed(2)}</span>`:'';const map=(row.driver_latitude!=null&&row.driver_longitude!=null)?`<a class="bct-map-link" target="_blank" rel="noopener" href="https://www.google.com/maps?q=${encodeURIComponent(row.driver_latitude+','+row.driver_longitude)}">📍 مشاهدة موقع المندوب</a>`:'';const updated=row.driver_location_updated_at?`<div class="bct-note">آخر تحديث للموقع: ${new Date(row.driver_location_updated_at).toLocaleString('ar-OM')}</div>`:'';return `<div class="bct-driver"><div class="bct-driver-top"><strong>🛵 ${esc(row.driver_name||'المندوب')}</strong><span>${online}</span></div><div class="bct-driver-meta">${phone}${vehicle}${rating}<span>الحالة: ${esc(row.assignment_status||row.order_status||'')}</span></div>${map}${updated}</div>`;}
  function orderHtml(row){const status=row.order_status||'pending';const statusClass=status==='delivered'?'done':status==='cancelled'?'cancel':'';return `<article class="bct-order" data-order-number="${esc(row.order_number||'')}"><div class="bct-top"><div class="bct-no">#${esc(row.order_number||row.order_id)}</div><span class="bct-status ${statusClass}">${esc(statusLabels[status]||status)}</span></div>${timeline(status)}<div class="bct-grid"><div class="bct-info"><small>الإجمالي</small><strong>${Number(row.total||0).toFixed(3)} OMR</strong></div><div class="bct-info"><small>عنوان التوصيل</small><strong>${esc(row.delivery_address||'—')}</strong></div></div>${driverBlock(row)}</article>`;}
  async function loadTracking(silent=true){const section=ensureSection();if(!section||!sb?.auth||!sb?.rpc||loading)return;loading=true;const host=section.querySelector('#bct-content'),refreshBtn=section.querySelector('#bct-refresh');if(!silent){refreshBtn?.classList.add('busy');if(refreshBtn)refreshBtn.textContent='جاري التحديث...';}try{const {data:{session}}=await sb.auth.getSession();if(!session?.user){host.innerHTML='<div class="bct-empty">سجّل الدخول لمتابعة طلباتك.</div>';return;}const {data,error}=await sb.rpc('customer_delivery_tracking');if(error)throw error;let rows=data;if(typeof rows==='string')rows=JSON.parse(rows||'[]');rows=Array.isArray(rows)?rows:[];if(!rows.length){host.innerHTML='<div class="bct-empty">لا توجد طلبات للمتابعة حاليًا.</div>';return;}const snapshot=JSON.stringify(rows.map(r=>[r.order_id,r.order_status,r.driver_id,r.driver_name,r.assignment_status,r.driver_online,r.driver_location_updated_at,r.delivery_address]));if(host.dataset.snapshot!==snapshot){host.innerHTML=rows.map(orderHtml).join('');host.dataset.snapshot=snapshot;}section.querySelector('#bct-last-check').textContent='آخر تحديث: '+new Date().toLocaleString('ar-OM');}catch(e){console.error('BINGO customer tracking RPC:',e);host.innerHTML='<div class="bct-error">تعذر تحميل متابعة الطلب: '+esc(e?.message||String(e))+'</div>';}finally{loading=false;if(!silent){refreshBtn?.classList.remove('busy');if(refreshBtn)refreshBtn.textContent='تحديث';}}}
  function queueRealtimeRefresh(){clearTimeout(reloadTimer);reloadTimer=setTimeout(()=>loadTracking(true),180);}
  function startRealtime(){if(!sb?.channel||realtimeChannel)return;realtimeChannel=sb.channel('bingo-customer-delivery-live-'+Math.random().toString(36).slice(2)).on('postgres_changes',{event:'*',schema:'public',table:'delivery_orders'},queueRealtimeRefresh).on('postgres_changes',{event:'*',schema:'public',table:'delivery_assignments'},queueRealtimeRefresh).on('postgres_changes',{event:'UPDATE',schema:'public',table:'delivery_drivers'},queueRealtimeRefresh).subscribe(status=>{const live=document.getElementById('bct-live');if(!live)return;if(status==='SUBSCRIBED'){live.textContent='● مباشر';live.style.color='#187845';}else if(status==='CHANNEL_ERROR'||status==='TIMED_OUT'){live.textContent='● إعادة اتصال';live.style.color='#a85a00';}});}
  function stopRealtime(){if(realtimeChannel&&sb?.removeChannel)sb.removeChannel(realtimeChannel);realtimeChannel=null;}
  function init(){addStyles();ensureSection();loadTracking(false);startRealtime();setInterval(()=>loadTracking(true),30000);if(sb?.auth?.onAuthStateChange)sb.auth.onAuthStateChange((_event,session)=>setTimeout(()=>{if(session){startRealtime();loadTracking(true);}else stopRealtime();},300));window.addEventListener('beforeunload',stopRealtime,{once:true});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();