(function(){'use strict';if(!/bingo-delivery-seller\.html$/i.test(location.pathname))return;const sb=window.sb;if(!sb)return;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),unit=u=>({piece:'قطعة',kg:'كغ',g:'غرام',meter:'متر',liter:'لتر',pack:'عبوة'}[u]||'قطعة'),qty=(v,u)=>{const n=Number(v||0);return ['piece','pack'].includes(u)?String(Math.trunc(n)):n.toFixed(3).replace(/\.000$/,'').replace(/(\.\d*[1-9])0+$/,'$1')};let busy=false;async function apply(){if(busy)return;const cards=[...document.querySelectorAll('.seller-order')];if(!cards.length)return;busy=true;try{for(const card of cards){const no=(card.querySelector('.seller-order-head strong')?.textContent||'').trim();if(!no)continue;const or=await sb.from('delivery_orders').select('id').eq('order_number',no).maybeSingle();if(or.error||!or.data?.id)continue;const ir=await sb.from('delivery_order_items').select('description,quantity,unit_price,unit').eq('order_id',or.data.id);if(ir.error)continue;const ul=card.querySelector('.seller-items');if(!ul)continue;ul.innerHTML=(ir.data||[]).map(i=>`<li><strong>${esc(i.description)}</strong> × ${qty(i.quantity,i.unit)} ${unit(i.unit)} — ${Number(i.unit_price||0).toFixed(3)} OMR / ${unit(i.unit)}</li>`).join('')}}finally{busy=false}}function boot(){apply();new MutationObserver(()=>setTimeout(apply,80)).observe(document.getElementById('seller-orders')||document.body,{childList:true,subtree:true});setInterval(apply,7000)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();})();