(function(){'use strict';
const page=(location.pathname.split('/').pop()||'').toLowerCase();if(page!=='dashboard.html')return;
function decorate(){document.querySelectorAll('#myListings .my-ad-card').forEach(card=>{if(card.dataset.promoteReady)return;const view=card.querySelector('a[href*="listing.html?id="]');const actions=card.querySelector('.my-ad-actions');const badge=card.querySelector('.ad-status');if(!view||!actions||!badge)return;const published=badge.classList.contains('published')||/published/i.test(badge.textContent||'');if(!published)return;const u=new URL(view.href,location.href),id=u.searchParams.get('id');if(!id)return;const a=document.createElement('a');a.className='bingo-promote-btn';a.href='listing-promote.html?id='+encodeURIComponent(id);a.textContent='⭐ Promote';a.title='Promote this listing';actions.appendChild(a);card.dataset.promoteReady='1'})}
function start(){decorate();const box=document.getElementById('myListings');if(box)new MutationObserver(decorate).observe(box,{childList:true,subtree:true});let tries=0;const timer=setInterval(()=>{decorate();if(++tries>30)clearInterval(timer)},250)}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();
})();