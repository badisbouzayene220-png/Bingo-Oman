{
  "name": "bingo-oman-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "BINGO Oman Capacitor mobile build generated from the current web release.",
  "scripts": {
    "mobile:prepare": "node scripts/mobile-prepare-web.mjs",
    "mobile:android": "npm run mobile:prepare && npx cap add android && node scripts/mobile-patch-native.mjs android && npx cap sync android",
    "mobile:ios": "npm run mobile:prepare && npx cap add ios && node scripts/mobile-patch-native.mjs ios && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/cli": "8.5.0",
    "@capacitor/ios": "8.5.0"
  }
}
